from django.urls import path

# Placeholder for future discount API endpoints
# For MVP, discounts are managed only via Django Admin

urlpatterns = [
    # Future: coupon validation endpoint
    # path('coupons/validate/', ...),
]
